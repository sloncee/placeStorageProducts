# placeStorageProducts

Java-проект, моделирующий хранение различных товаров на складе с применением принципов объектно-ориентированного программирования.

## 🧱 Структура модели

Проект включает следующие основные сущности:

- `Product` — базовая сущность товара
- `Stack` — класс для описания характеристик полки
- `ProductOnStack` — полка с товаром (имеет наследников)
    - `ClothingProductOnStack`
    - `FoodProductOnStack`
- `Warehouse` — абстрактный класс склада с товарами
    - `BookWarehouse`
    - `MedicineWarehouse`
- Интерфейс `Interface` — содержит метод `print()` для унифицированного вывода

## 🔥 Исключения

- `UncheckedProductOnStackPriceException` — unchecked-исключение при отрицательной цене
- `CheckedWarehouseAddProductException` — checked-исключение при попытке добавления товара в недопустимое место

## 🧰 Сервисы

Проект включает два сервиса:

- `ServiceWithCatch` — обработка исключений внутри методов
- `ServiceWithoutCatch` — исключения пробрасываются вызывающему коду

## 🧮 Компаратор

- `ProviderProductOnStackComparator` — компаратор для сортировки товаров по поставщику

## 🧪 Тестирование

Модульное тестирование реализовано с использованием **JUnit 5** и **AssertJ**:

- Тесты сервисов:
    - `ServiceWithCatchTest`
    - `ServiceWithoutCatchTest`
- Тесты компаратора:
    - `SortProductOnStackTest`

Покрываются сценарии:

- добавление товаров (валидные и невалидные случаи)
- проверка обработки исключений
- сортировка по полям

## ⚙️ Технологии и инструменты

- **Java 17+**
- **Maven**
- **JUnit 5** 
- **AssertJ** 
- **Log4j** 
